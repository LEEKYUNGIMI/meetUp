[phases.setup]
nixPkgs = ["jdk21_headless"]

[phases.build]
cmds = ["cd backend && chmod +x gradlew && ./gradlew bootJar -x test"]

[start]
cmd = "java -jar backend/build/libs/backend-0.0.1-SNAPSHOT.jar"