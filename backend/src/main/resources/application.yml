spring:
  datasource:
    url: jdbc:mysql://shortline.proxy.rlwy.net:26418/railway?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: RuRgiDuTrlCTnrcRMXypHszldtKEQOUY
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${PORT:8080}